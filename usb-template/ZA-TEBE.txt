TERMINAI — NAVODILA ZA NAMESTITELJA (te)
==========================================

VSEBINA USB:
  TERMINAI/          <- to skopiras na racunalnik stranke (NAMESTI.bat)
    app/             <- aplikacija + SQLite baza (demo: Studio Aura)
    runtime/         <- bun.exe (Windows) + bun-linux (Linux)
    ZAGON.bat        <- zagon (port 3456)
    NAMESTI.bat      <- namestitev v C:\TerminAI + bližnjica
    NAREDI-REZERVO.bat <- backup baze na USB
    start.sh         <- Linux/Mac zagon
    NAVODILA.txt     <- navodila, ki jih pustis stranki

POSTUPOK PRI STRANKI:
  1. Vstavi USB, pozeni NAMESTI.bat (skopira vse v C:\TerminAI).
  2. Aplikacija se zazene — brskalnik se odpre.
  3. Zavihek "Nadzorna plosca lastnika" > "Storitve & salon":
     klikni "Nastavi pravi salon" — vpisi NJIHOVE podatke.
  4. Dodaj NJIHOVE storitve (ime, trajanje, redna + vršna cena).
  5. Test z 2 telefonom: telefon na NJIHOV WiFi ->
     http://<IP-racunalnika>:3456 (IP izpise ZAGON.bat / ipconfig)
  6. Stranka naj naredi 1-2 rezervaciji, potrdi ju v plosci.
  7. PRED ODHODOM: pozeni NAREDI-REZERVO.bat (backup na USB).

FIREWALL (Windows):
  Ob prvem zagonu Windows vprasa -> "Dovoli dostop" + kljukica
  ZASEBNA omrezja. Brez tega telefoni strank ne dostopajo.
  Ce okno ni prislo: Control Panel > Firewall > Allowed apps > bun.

TEHNINE PODATKI:
  - Port: 3456 (localhost + LAN 0.0.0.0)
  - Baza: SQLite, app/custom.db (portabilna, backup = copy)
  - AI: IZKLOPLJENA v tem paketu (offline build, 0 stroškov)
  - Fonti/favicon: vsi lokalni, nobenega CDNa — deluje brez interneta
  - Update: skopiraj novo app/ mapo prek stare (bazo NE prekrij,
    razen ce zelis reset!)

PREHOD ONLINE (naslednja faza):
  - baza custom.db se uvozi na streznik (enaka shema)
  - AI zastavica se vklopi (Ana)
  - domena + SMS spomniki

TESTIRANO: build 2026-09-03 (glej worklog.md).

NOVOSTI (namestitelj):
  - Gumb WHATSAPP na rezervacijski strani (wa.me klik-za-klepet,
    zastonj, telefonska stevilka salona iz baze — Business.phone).
  - "Dodaj termin" v nadzorni ploi: rocni vnos za rezervacije,
    pritele po telefonu/WhatsAppu. Enak API kot javni widget.
  - QR koda v nadzorni ploi (qrcode.react, offline, trenutni naslov).
  - Pri prodaji: pokaite QR kodo stranki v salonu — takoj demo ucinek.

NOVOST 2 (namestitelj):
  - Modul "Sporocila": parser slovenskih sporoil (namen: naroilo/
    cena/cenik/zasedenost; storitve iz baze; "jutri", "v soboto",
    "ob 10h"). Deluje 100% lokalno, brez AI strokov.
  - POST /api/messages razcleni + sestavi odgovor + shrani zgodovino
    (nova tabela Message). GET vrne zadnjih 50.
  - PATCH /api/setup (mode:edit): urejanje podatkov salona brez reset.
  - Pri prodaji: demo potek "stranka pie -> program odgovori ->
    termin vpie" naredi najvecji ucinek. Pokazite s primeri na zaciku.

NOVOST 3 (namestitelj):
  - PONAVLJAJOI TERMINI: "Dodaj termin" ima izbiro "Ponavljajoi
    obisk" (2/3/4/6/8 tednov). Kartica "Ponavljajoi obiski — kdo je
    na vrsti" v nadzorni ploi samodejno pokaže, koga poklicati
    (rok potekel / na vrsti / kmalu / e naroena), z WhatsApp vabilom
    in gumbom NAROI. GET /api/appointments/recurrence.
  - SAMODEJNE VARNOSTNE KOPIJE: ob vsakem zagonu (max 1x dnevno)
    VACUUM INTO snapshot v app/db/backups (zadnjih 14). Kartica
    "Varnostne kopije" v zavihku Storitve & salon: seznam, ročna
    kopija, PRENESI na USB. API: GET/POST /api/backup.
  - Pri prodaji: pokaite "kdo je na vrsti" — frizerkam takoj klikne
    (papirni koledar tega NE ZNA). Samodejni backup = manj podpore
    zate (manj klicev ob okvari diska).

NOVOST 4 (namestitelj):
  - ODPOVED PREK POVEZAVE: vsak termin ima enkratno kodo (cancelToken).
    Stranka klikne povezavo (/?cancel=token) iz WhatsApp spomnika ali
    potrditve rezervacije -> vidi termin -> Odpovej -> termin se sprosti.
    Brez PIN-a (token je overitev), javni API /api/appointments/cancel.
    Gumb za kopiranje povezave v koledarju (ikona verige).
  - NO-SHOW SLEDENJE: gumb "Ni prila" na pretekelih terminih (poleg
    Zakljui). Rdeca oznaka v terminu + stevilo izostankov pri stranki
    v zavihku Stranke ("2x ni prila"). Ne steje se v obiske/prihodke.
  - ICAL IZVOZ: gumb v glavi koledarja (puca dol) prenese
    terminai-koledar.ics (zadnjih 30 dni + naslednjih 6 mesecev).
    Uvoz v Google/Apple Koledar. API: GET /api/appointments/ical (PIN).
  - Varnost: GET /api/appointments zdaj zahteva PIN (telefoni strank
    niso vec javni). Javni POST rezervacije seveda ostaja odprt.
  - Pri prodaji: odpovedna povezava = "stranka odpove sama, ti nic
    ne pocnes" + no-show stevec = "vidis, kdo te nastavlja" — dve
    zgodbri, ki papirni koledar ne zna povedati.
