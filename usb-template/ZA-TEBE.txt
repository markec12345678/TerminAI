TERMINAI — NAVODILA ZA NAMESTITELJA (te)
==========================================

VSEBINA USB:
  TERMINAI/          <- to skopiras na racunalnik stranke (NAMESTI.bat)
    app/             <- aplikacija + SQLite baza (demo: Studio Aura)
    runtime/         <- bun.exe (Windows) + bun-linux (Linux)
    ZAGON.bat        <- zagon (port 3456)
    NAMESTI.bat      <- namestitev v C:\TerminAI + bližnjica
    NAREDI-REZERVO.bat <- backup baze na USB
    start.sh         <- Linux/Mac zagon
    NAVODILA.txt     <- navodila, ki jih pustis stranki

POSTUPOK PRI STRANKI:
  1. Vstavi USB, pozeni NAMESTI.bat (skopira vse v C:\TerminAI).
  2. Aplikacija se zazene — brskalnik se odpre.
  3. Zavihek "Nadzorna plosca lastnika" > "Storitve & salon":
     klikni "Nastavi pravi salon" — vpisi NJIHOVE podatke.
  4. Dodaj NJIHOVE storitve (ime, trajanje, redna + vršna cena).
  5. Test z 2 telefonom: telefon na NJIHOV WiFi ->
     http://<IP-racunalnika>:3456 (IP izpise ZAGON.bat / ipconfig)
  6. Stranka naj naredi 1-2 rezervaciji, potrdi ju v plosci.
  7. PRED ODHODOM: pozeni NAREDI-REZERVO.bat (backup na USB).

FIREWALL (Windows):
  Ob prvem zagonu Windows vprasa -> "Dovoli dostop" + kljukica
  ZASEBNA omrezja. Brez tega telefoni strank ne dostopajo.
  Ce okno ni prislo: Control Panel > Firewall > Allowed apps > bun.

TEHNINE PODATKI:
  - Port: 3456 (localhost + LAN 0.0.0.0)
  - Baza: SQLite, app/custom.db (portabilna, backup = copy)
  - AI: IZKLOPLJENA v tem paketu (offline build, 0 stroškov)
  - Fonti/favicon: vsi lokalni, nobenega CDNa — deluje brez interneta
  - Update: skopiraj novo app/ mapo prek stare (bazo NE prekrij,
    razen ce zelis reset!)

PREHOD ONLINE (naslednja faza):
  - baza custom.db se uvozi na streznik (enaka shema)
  - AI zastavica se vklopi (Ana)
  - domena + SMS spomniki

TESTIRANO: build 2026-09-03 (glej worklog.md).

NOVOSTI (namestitelj):
  - Gumb WHATSAPP na rezervacijski strani (wa.me klik-za-klepet,
    zastonj, telefonska stevilka salona iz baze — Business.phone).
  - "Dodaj termin" v nadzorni ploi: rocni vnos za rezervacije,
    pritele po telefonu/WhatsAppu. Enak API kot javni widget.
  - QR koda v nadzorni ploi (qrcode.react, offline, trenutni naslov).
  - Pri prodaji: pokaite QR kodo stranki v salonu — takoj demo ucinek.

NOVOST 2 (namestitelj):
  - Modul "Sporocila": parser slovenskih sporoil (namen: naroilo/
    cena/cenik/zasedenost; storitve iz baze; "jutri", "v soboto",
    "ob 10h"). Deluje 100% lokalno, brez AI strokov.
  - POST /api/messages razcleni + sestavi odgovor + shrani zgodovino
    (nova tabela Message). GET vrne zadnjih 50.
  - PATCH /api/setup (mode:edit): urejanje podatkov salona brez reset.
  - Pri prodaji: demo potek "stranka pie -> program odgovori ->
    termin vpie" naredi najvecji ucinek. Pokazite s primeri na zaciku.

NOVOST 3 (namestitelj):
  - PONAVLJAJOI TERMINI: "Dodaj termin" ima izbiro "Ponavljajoi
    obisk" (2/3/4/6/8 tednov). Kartica "Ponavljajoi obiski — kdo je
    na vrsti" v nadzorni ploi samodejno pokaže, koga poklicati
    (rok potekel / na vrsti / kmalu / e naroena), z WhatsApp vabilom
    in gumbom NAROI. GET /api/appointments/recurrence.
  - SAMODEJNE VARNOSTNE KOPIJE: ob vsakem zagonu (max 1x dnevno)
    VACUUM INTO snapshot v app/db/backups (zadnjih 14). Kartica
    "Varnostne kopije" v zavihku Storitve & salon: seznam, ročna
    kopija, PRENESI na USB. API: GET/POST /api/backup.
  - Pri prodaji: pokaite "kdo je na vrsti" — frizerkam takoj klikne
    (papirni koledar tega NE ZNA). Samodejni backup = manj podpore
    zate (manj klicev ob okvari diska).

NOVOST 4 (namestitelj):
  - ODPOVED PREK POVEZAVE: vsak termin ima enkratno kodo (cancelToken).
    Stranka klikne povezavo (/?cancel=token) iz WhatsApp spomnika ali
    potrditve rezervacije -> vidi termin -> Odpovej -> termin se sprosti.
    Brez PIN-a (token je overitev), javni API /api/appointments/cancel.
    Gumb za kopiranje povezave v koledarju (ikona verige).
  - NO-SHOW SLEDENJE: gumb "Ni prila" na pretekelih terminih (poleg
    Zakljui). Rdeca oznaka v terminu + stevilo izostankov pri stranki
    v zavihku Stranke ("2x ni prila"). Ne steje se v obiske/prihodke.
  - ICAL IZVOZ: gumb v glavi koledarja (puca dol) prenese
    terminai-koledar.ics (zadnjih 30 dni + naslednjih 6 mesecev).
    Uvoz v Google/Apple Koledar. API: GET /api/appointments/ical (PIN).
  - Varnost: GET /api/appointments zdaj zahteva PIN (telefoni strank
    niso vec javni). Javni POST rezervacije seveda ostaja odprt.
  - Pri prodaji: odpovedna povezava = "stranka odpove sama, ti nic
    ne pocnes" + no-show stevec = "vidis, kdo te nastavlja" — dve
    zgodbri, ki papirni koledar ne zna povedati.

NOVOST 5 (namestitelj):
  - MESecNO POROOILO: zavihek "Poroila" — mesec (navigacija),
    KPI-ji (realizirano/priakovano/povpreeni obisk/odpovedi),
    graf prihodkov po dnevih, top 5 storitev/strank.
  - CSV ZA KNJIGOVOĐSTVO: gumb prenese TerminAI-porocilo-YYYY-MM.csv
    (samo zakljjueni obiski, podpiija, decimalna vejica, UTF-8 BOM
    — Excel brez pretvorb). API: GET /api/reports?format=csv (PIN).
  - DEMO NAIN: kartica "Demo podatki" v zavihku Salon — vpis DEMO
    (+PIN) obnovi Studio Aura z bogato zgodovino (~40 dni, ~100
    terminov, odpovedi, izostanki, ponavljanja). Za prodajne
    obiske: obnova -> predstavitev -> "Nastavi pravi salon".
    API: POST /api/setup {mode:"demo"} (PIN).
  - POPRAVEK: reset baze (mode=fresh) zdaj briše tudi WorkingHours
    (prej bi padlo na tuj klju) in sporoila.
  - POPRAVEK mobilnega prelivanja: min-w-0 na mrei koledarja +
    glava kartice se ovija + vrstice delovnega asa se ovijajo
    (375 px zdaj brez horizontalnega scrolla na vseh zavihkih).
  - Pri prodaji: CSV = "knjigovodi dobi isto datoteko kot od
    papirnega koledarja, samo brez raunanja" + demo obnova v
    10 sekundah pri vsakem obisku (vedno svee številke za predstavitev).

NOVOST 6 (namestitelj) — analiza vrzeli po konkurenci (Fresha/Booksy/Planity):
  - ZAPRTI DNEVI: prazniki (uvoz s klikom, slovenski prazniki za 2
    leti, velikonočni izračunani), dopust z obsegom, posamezni dnevi.
    Strankam so dnevi vidno prečrtani v traku; parser Sporočil
    odgovori "smo ZAPRTI (razlog)" + alternativni dnevi.
    API: GET /api/closed-days (javno), POST {add|add-range|holidays},
    DELETE ?date= (PIN). Model ClosedDay (date @unique, reason).
  - PREMOR (kosilo): WorkingHours.breakStart/breakEnd — slotovi
    preskočijo okno (npr. 12:00-13:00).
  - BUFFER: Service.bufferMin (0-30 min) — priprava/razkuževanje
    po storitvi, termin+priprava skupaj na vrtico.
  - OPOMBE O STRANKI: Client.notes zdaj dostopen v UI (svinčnik pri
    stranki; formule barvanja, alergije). PATCH /api/clients/[id].
  - GDPR: GET /api/clients/[id] = izvoz JSON (pravica dostopa),
    DELETE = stranka + termini (pravica izbrisa, s potrditvenim
    oknom v UI). Prodajni argument za pravne zahteve.
  - OBNOVA IZ KOPIJE: POST /api/backup {action:"restore", file} —
    $disconnect -> rename (atomarno) -> $connect; pred obnovo se
    vedno naredi zaščitna kopija (-2 priponka ob trku imen —
    VACUUM INTO zavrne obstoječo datoteko, odpravljen hrošč).
  - PWA: src/app/manifest.ts (ikona na domačem zaslonu).
  - Wipe (mode=fresh/demo) briše tudi ClosedDay; fresh setup in
    seedDemo samodejno uvozita praznike za tekoče+naslednje leto.
  - Pri prodaji: "sistem ve, kdaj ste zaprti — nobena stranka več
    ne rezervira na 1. maj" + "formule imate pri stranki, ne v
    zvezku" + "izbris podatkov po GDPR v 10 sekundah".

NOVOST 7 (namestitelj) — UX/workflow analiza po svetovnih vzorcih
(Fresha/Booksy/Meevo/Square; EU+USA: check-in statusi, zvocna
obvestila, 15-min zamujanje; AZIJA: super-app sporočanja):
  - ZVOCNA OPOZORILA: Web Audio API (brez datotek, 100% offline!)
    — src/lib/sounds.ts. Nova rezervacija = ding-dong, odpoved =
    mehak padajoc ton, prihod = pozdravni, zakljjuček = kratek.
    Vklop/izklop z gumbom (persistenca v localStorage).
  - POLLING (12 s): GET /api/appointments?since=<ISO> — Appointment
    dobl @updatedAt (db:push). Dashboard sam zazna rezervacije
    strank z vseh datumov + odpovedi prek povezav + tujih sprememb;
    lastnikove akcije se ne zvonejo (ownerActionsRef filter).
  - CHECK-IN "PRISLA JE": nov status checked_in (teal badge
    "Prisla") — workflow zdaj Rezervacija → Potrdi → Prisla je →
    Zakljjuči (kot Fresha "Arrived"). PATCH sprejema checked_in;
    stats/reports/porocila upostevajo nov status.
  - 15-MIN PRAVILO: rumen badge "ZAMUJA X MIN" na terminih, ki so
    se zaceli in stranka ni prijavljena (US standard politika).
  - STRANKI: mehek zvoncek potrditve po rezervaciji (delight).
  - E2E: toast "Nova rezervacija" prisel v 5 s po POST (curl),
    odpovedni toast v 5 s, stikalo persistira (on/off), check-in
    badge preklopi, mobilni 375 px 0 preliva, VLM 10/10.

NOVOST 8 — KOT DRAGI ZENOTI (po zenotijevskih receptih, brez naročnine)
- Formule pri strankah: ob zaključku obiska zapišete, kaj je bilo narejeno —
  Zenoti to šteje za svojo največjo prednost. Pri nas enako, 0 EUR/mesec.
- Zgodovina obiskov stranke z enega zaslona (datum, storitev, formula, cena).
- Naslednji termin dogovorite, še preden stranka odide (rebooking).
- Čakalni seznam: ko kdo odpove, program opomni koga poklicati (WhatsApp
  povabilo s klikom). Zenoti za to zaračunava premium paket.
- »Dolgo jih ni bilo«: seznam strank po 8 tednih brez obiska + WhatsApp
  sporočilo s klikom (Zenoti: AI Retention Manager, 450 $/mesec).
- Walk-in: stranka brez termina je na stolu v 10 sekundah.

NOVOST 9 — FOTOGRAFIJE IN ROJSTNI DNEVI (Zenoti Photo Manager + birthday, 0 EUR/mesec)
- Fotografija PRED/Po ob vsakem obisku: pomanjša se sama in shrani na
  VAŠ računalnik (nikoli v oblak — za razliko od Zenotija, ki slike
  hrani pri sebi in zaračunava 225–500 USD/mesec).
- Referenca: stranka prineše sliko "takšno frizuro hočem" — shranite
  jo pri njej, naslednjič v 1 sekundi vidite, kaj je mislila.
- Povečava z enim klikom v zgodovini stranke; GDPR izvoz vsebuje slike.
- Rojstni dnevi: kartica vam pove, kdo ima rojstni dan (danes/jutri/
  čez 3 dni) + pripravljena WhatsApp čestitka z vabilom (-20 % v
  rojstnem mesecu). Najbolj odprto sporočilo, ki ga stranka prejme.
